# fund-agent optional local analysis dependencies
#
# Install only if local demos or analysis notebooks need these:
#   pip install -r requirements-optional.txt
#
# These are NOT required for plugin install or host integration.
# They are local developer conveniences only.

numpy>=1.20.0
pandas>=1.3.0
networkx>=3.2
