# v0.10.5 Personal Plugin Freeze Notes

## Status

- **Head:** `169ef87`
- **Branch:** `t1.0` (open PR, not merged)
- **Tag:** none
- **Release:** none
- **Freeze:** personal-plugin functional freeze achieved

## Completed Capabilities

### Data Ingestion
- Alipay CSV import: 100 real transactions parsed in local real E2E
- Transaction ledger: 100 evidence-confirmed transactions
- Identity schema v2
- `fund_identity_overrides.private.yaml` support
- Valid six-digit fund code resolution
- NAV overrides (trade-date)

### Pipeline
- Reconstruction bridge
- `valuation_type` state machine:
  - `none` — no data
  - `cashflow_only` — transactions parsed, no NAV
  - `estimated` — partial NAV available
  - `valuation` — confirmed source only
- Partial valuation coverage
- `existing_private_portfolio_input` fallback

### Report Semantics
- No fake `0.00` — unknown values are `None`, not zero
- Cashflow != valuation — clearly separated
- Estimated != confirmed — distinct labels
- Fallback != `reconstructed_from_ledger` — no misleading wording
- `transactions_only` source label distinct from `existing_private_portfolio_input`

## Local Gates

| Gate | Result |
|------|--------|
| `test_fast` | 2338 passed, 1 skipped |
| `test_identity_nav` | 70 passed |
| `test_plugin_smoke` | 236 passed, 1 skipped |
| Privacy check | PASS |
| Workflow/report/public_api/architecture | 438 passed, 1 skipped |
| Full pytest | 4177 passed, 16 skipped, 1 warning |

## Real E2E Desensitized Matrix

| Scenario | CSV | Identity Overrides | NAV Overrides | Portfolio Input Fallback | Status |
|----------|-----|--------------------|---------------|--------------------------|--------|
| A | yes | no | no | no | partial |
| B | yes | yes | no | no | partial |
| C2 | yes | yes | yes (trade-date) | no | partial |
| D2 | yes | yes | no | yes (existing) | partial |

### Scenario A: CSV only, no overrides
- `valid_fund_codes_count` = 0
- `name_only_count` > 0
- Warnings: fund code mapping required, no valid six-digit fund codes, NAV/reconstruction skipped
- `resolved_fund_code` for Chinese names remains null

### Scenario B: CSV + identity overrides, no NAV
- `valid_fund_codes_count` > 0
- `reconstruction_status` = `nav_unavailable`
- `portfolio_input_source` = `unavailable`
- Warnings: identity resolved but NAV data missing, valuation unavailable

### Scenario C2: CSV + identity overrides + trade-date NAV overrides
- Full identity + NAV resolution
- `reconstruction_status` = `reconstructed_from_ledger`
- `portfolio_input_source` = `reconstructed_from_ledger`

### Scenario D2: existing portfolio_input fallback
- Cashflow source: Alipay transactions (`transactions_only`)
- Valuation source: `existing_private_portfolio_input`
- Reconstruction: unavailable/skipped
- No `reconstructed_from_ledger` wording

## Out of Scope for This Freeze

- Workflow/report architecture changes
- File splitting or structural refactoring
- Identity/NAV/reconstruction algorithm changes
- Remote CI modifications
- `portfolio_input.transactions` fallback
- Provider search
- Broker/order execution
