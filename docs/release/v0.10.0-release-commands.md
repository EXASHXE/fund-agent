# v0.10.0 Release Commands

```bash
# Preflight
git status --short

# Full test gate
PYTHONPATH=. pytest -q

# Personal regressions
PYTHONPATH=. python -c "from fund_agent.cli import main; main()" regressions --pretty

# Demo markdown report
PYTHONPATH=. python -c "from fund_agent.cli import main; main()" analyze-portfolio \
  --input examples/user_portfolio_templates/fund_portfolio_input_demo.json \
  --format markdown \
  --output /tmp/fund-agent-demo-report.md

# Smoke host install
PYTHONPATH=. python scripts/smoke_host_install.py

# Audits
PYTHONPATH=. python scripts/audit/run_all_audits.py --pretty

# Tag
git tag -a v0.10.0 -m "fund-agent v0.10.0 beta candidate"
git push origin v0.10.0
git push origin master
```
