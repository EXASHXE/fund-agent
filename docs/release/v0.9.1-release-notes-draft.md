# v0.9.1 Release Notes Draft

> Not yet tagged. Prepare only.

## What was fixed

- Stabilized provider adapter tests so default pytest does not require live AkShare, network, or environment credentials.
- Added `live_provider` / `adapter_live` test markers with explicit opt-in via `FUND_AGENT_RUN_LIVE_PROVIDER_TESTS` environment variable.
- Added live provider adapter test file (`tests/host_data/test_live_provider_adapters.py`) — skipped by default.
- Clarified live provider smoke tests as opt-in with CI warning in adapter README.
- Added development testing guide (`docs/development/testing.md`).
- Documented v0.9.0 AkShare env failure root cause in stabilization notes.

## Tests now clean by default

- Default `pytest -q` has 0 failures.
- Live provider tests are properly skipped by default.
- All adapter unit tests remain fully mocked (no network, no real SDK).

## No changes

- No behavior/provider/broker changes.
- Core runtime remains no-network and no-provider-SDK.
- Live provider smoke remains outside core runtime and opt-in.
