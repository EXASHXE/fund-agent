# v0.10.6 Architecture Cleanup Backlog

Items deferred from the t1.0 optimization pass (v0.10.5 post-freeze).

## 1. Full Workflow Boundary Cleanup

**Status**: Partially done in t1.0 (tools/workflow no longer imports skills_runtime).

**Remaining**:
- Audit all `src.skills_runtime.workflow` imports of `src.tools.workflow` —
  ensure only public functions are imported, not internal helpers.
- Consider moving `evidence_bridge.py`'s `convert_host_*` functions to a
  dedicated adapter location if they grow in scope.
- Evaluate whether `advisory_markdown_adapter.py` and `markdown_report.py`
  should be consolidated into a single `advisory_report` subpackage.
- Public facade `src/fund_agent/workflow.py` should be the only composition
  point; document this in `docs/architecture/`.

## 2. Advisory Report Pipeline Consolidation

**Status**: Helper dedup done in t1.0 (`_theme_text`, `_dedupe_preserve_order`
extracted to `report_helpers.py`; `_unique_strings` delegated to
`common/strings.py`).

**Remaining**:
- Unify section registry across `portfolio/report_sections/` and
  `tools/workflow/final_report.py` — both have section builder patterns
  that could share a registry abstraction.
- Add golden snapshot tests for advisory report output to catch
  unintentional semantic changes.
- Unify Chinese/English report renderer abstraction — currently
  `report_zh.py` and `markdown_report.py` have parallel rendering logic.
- `_find_section` has two signatures (dict-index vs list-scan) in
  `advisory_markdown_adapter.py` and `markdown_report.py` — consider
  unifying or documenting why they differ.

## 3. Large File Split

**Status**: `advisory_quality_gate.py` partially split in t1.0
(`compute_quality_status` extracted to `quality_status.py`, 832→610+99).

**Remaining files to split**:
- `src/tools/portfolio/report_sections/builders.py` (1084 lines) — split
  by section type (allocation, valuation, risk, cashflow, reconstruction).
- `src/tools/portfolio/ledger_snapshot.py` (965 lines) — split into
  normalize/build/reconcile/pnl/settlement submodules.
- `src/skillpack/run_skill.py` (952 lines) — separate CLI parsing,
  execution, and output rendering.
- `src/skillpack/input_contracts.py` (860 lines) — registry-ize contract
  definitions by type.
- `src/graph/builder.py` (826 lines) — split by build phase.
- `src/tools/workflow/final_report.py` (~1010 lines after t1.0) — extract
  `_build_*_section` functions into section submodules.
- `src/tools/workflow/advisory_quality_gate.py` (610 lines after t1.0) —
  consider registry pattern for the 11 `_check_*` functions.

Each split must preserve public import paths via re-export shims and pass
golden/public API regression tests.

## 4. Full-Project Ruff Debt

**Status**: v0.10.5 uses canonical lint scope only
(`scripts/lint_release_scope.sh`).

**Remaining**:
- ~590 historical lint issues in `examples/`, `src/graph/`, and other
  non-critical paths.
- Run `ruff check . --fix` on non-critical paths.
- Consider enabling additional ruff rules (C4, PT, RET) incrementally.
- `src/tools/registry.py` has Chinese error messages with encoding issues
  on some terminals — standardize to English or add explicit i18n.

## 5. portfolio_input.transactions Fallback

**Status**: Not addressed in v0.10.5.

**Remaining**:
- Treat `portfolio_input.transactions` as a first-class transaction source
  alongside Alipay CSV and manual entries.
- Currently the pipeline does not use `portfolio_input.transactions` as
  a transaction ledger input — it only uses position-level data.
- This would enable cost-basis reconstruction without a separate CSV.

## 6. Provider / NAV Enhancement

**Status**: Not addressed in v0.10.5.

**Remaining**:
- Multi-provider fallback chain (if provider A fails, try provider B).
- QDII delay confirmation — QDII funds have T+1/T+2 NAV publication;
  the pipeline should clearly mark delayed NAVs.
- Fee schedule, conversion, and redemption handling refinements.
- Consider adding a lightweight public-data fetcher as an optional
  built-in MCP adapter example, so users can analyze arbitrary fund
  codes without configuring external MCP providers.
