请重构当前 fund-agent 的目录结构，不改变业务行为，目标是把新 Research OS 主路径和旧 legacy 系统彻底分层。当前 src 顶层目录过多，新旧逻辑混杂。请按以下目标树整理：src/  os/  core/  schemas/  graph/  tools/    quant/    ledger/    evidence/    adapters/  infra/    data/    persistence/    vectorstore/    config/  workflows/legacy/  cli/  workflows/  analysis/  news/  recommend/  output/  ui/  routes/  prompts/  deprecated/迁移规则：1. 新 Research OS 主路径必须留在 src：   - src/core/*   - src/schemas/*   - src/workflows/research_os.py   - KnowledgeGraph 相关代码   - EvidenceGraph 相关代码   - Quant/Ledger/Evidence 纯工具2. 把旧系统迁入 legacy：   - src/cli.py 的旧 analyze 入口   - src/workflows/analyze.py   - src/output/*   - src/recommend/*   - src/ui/*   - src/routes/*   - src/prompts/*   - src/deprecated/*   - 旧 news pipeline   - 旧 scoring pipeline   - 旧 strategy state machine   - 旧 engine event workflow3. 不要把仍被新系统依赖的纯工具搬进 legacy。   如果旧 analysis/engine 里有 Sharpe、Sortino、MDD、Volatility、HHI、XIRR、DCA、settlement，请抽到：   - src/tools/quant/   - src/tools/ledger/4. 合并 src/kg 和 src/graph。   统一保留 src/graph。   src/kg 只保留兼容 shim，或在所有 import 更新后删除。5. 新增 src/infra/：   - 原 src/data -> src/infra/data   - 原 src/db -> src/infra/persistence   - 原 src/vectorstore -> src/infra/vectorstore   - 原 src/config -> src/infra/config6. 保留兼容 shim，避免一次性破坏旧命令：   - src/cli.py 可以暂时 re-export legacy.cli.cli:main   - src/kg 可以暂时 re-export src.graph   - 兼容 shim 必须标注 deprecated7. 新增 architecture boundary tests：   - src/core 不允许 import legacy   - src/os 不允许 import legacy   - src/workflows/research_os.py 不允许 import legacy   - src/tools 不允许 import legacy   - src/schemas 不允许 import legacy   - src/graph 不允许 import legacy   - src/tools 不允许 import requests / akshare / network SDK / LLM client   - legacy 可以 import src/tools、src/schemas，但 src 不可以反向 import legacy8. 更新 README：   - 明确 src 是新 Research OS   - legacy 是旧 CLI/report/news/recommend/ui 系统   - CLI analyze 是 legacy compatibility path   - Research OS 是新主路径9. 所有 import 必须更新。   所有测试必须通过：   PYTHONPATH=. pytest10. 不要删除旧功能。    本轮只做结构隔离和 import 边界，不做业务重写。