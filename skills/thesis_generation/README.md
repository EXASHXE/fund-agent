# thesis_generation Compatibility Directory

This underscore directory is compatibility-only for older Python imports and
tests. It is not a canonical Markdown skill doc directory and must not be
presented as a second runtime skill.

Use runtime skill ID `thesis_generation` from
`skillpack/fund-agent.skillpack.yaml`, and read canonical Markdown policy from
`skills/thesis-generation/SKILL.md`.
