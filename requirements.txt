# fund-agent minimal runtime requirements
#
# This file is the DEFAULT install surface for any host that
# runs `pip install -e .` or relies on requirements.txt.
#
# policy:
#   - Default install does NOT install provider SDKs.
#   - Provider SDKs belong to host implementations.
#   - requirements-legacy.txt is historical/reference only, not default.
#   - requirements-dev.txt contains test/dev tooling.
#   - requirements-optional.txt contains local analysis helpers
#     (numpy, pandas, networkx) for local demos if needed.

pyyaml>=6.0
